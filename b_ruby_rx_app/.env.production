# Production Environment Configuration for GCP
NODE_ENV=production
PORT=8080

# Main Database Configuration (Cloud SQL PostgreSQL)
DB_HOST=127.0.0.1
DB_USER=${MAIN_DB_USER}
DB_PASSWORD=${MAIN_DB_PASSWORD}
DB_NAME=${MAIN_DB_NAME}
DB_PORT=5432

# Medicine Database Configuration (Cloud SQL PostgreSQL)
MEDICINE_DB_HOST=127.0.0.1
MEDICINE_DB_USER=${MEDICINE_DB_USER}
MEDICINE_DB_PASSWORD=${MEDICINE_DB_PASSWORD}
MEDICINE_DB_NAME=${MEDICINE_DB_NAME}
MEDICINE_DB_PORT=5432

# Redis Configuration (Cloud Memorystore)
REDIS_URL=${REDIS_CONNECTION_STRING}

# JWT Configuration
JWT_SECRET=${JWT_SECRET_KEY}

# Google OAuth Configuration
GOOGLE_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET}
GOOGLE_REDIRECT_URI=${GOOGLE_OAUTH_REDIRECT_URI}

# LinkedIn OAuth Configuration
LINKEDIN_CLIENT_ID=${LINKEDIN_OAUTH_CLIENT_ID}
LINKEDIN_CLIENT_SECRET=${LINKEDIN_OAUTH_CLIENT_SECRET}
LINKEDIN_REDIRECT_URI=${LINKEDIN_OAUTH_REDIRECT_URI}

# Email Configuration
EMAIL_USER=${GMAIL_USER}
GOOGLE_EMAIL_USER=${GMAIL_USER}
GOOGLE_APP_PASSWORD=${GMAIL_APP_PASSWORD}

# WhatsApp Sender API
WASENDER_API_KEY=${WHATSAPP_API_KEY}

# Google Cloud Configuration
GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/gcp-service-account.json
GOOGLE_CLOUD_PROJECT_ID=${GCP_PROJECT_ID}

# Google Generative AI
GOOGLE_GENERATIVE_AI_API_KEY=${GENAI_API_KEY}